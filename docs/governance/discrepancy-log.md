# Discrepancy log

A discrepancy is any case where a document, spec or handover says one thing and the code or live system does another. Each entry gets one ruling. Entries are not deleted; a resolved entry keeps its ruling.

Historical entries recorded before 2026-09-23 remain in the Open Pins section of [`decisions/ontology-decisions.md`](decisions/ontology-decisions.md#open-pins) until DOC-2 moves them here. New entries go here only.

## Rulings

| Ruling | Meaning |
|---|---|
| `ratified deviation` | The implementation is correct; the spec is amended to match |
| `defect` | The spec is correct; the implementation must change |
| `conditional` | Ratified only if a named condition passes; otherwise a defect |
| `open` | Not yet ruled |

## Entries

| ID | Found | Source says | Code / live does | Ruling | Follow-up |
|---|---|---|---|---|---|
| DL-2026-09-23-01 | 2026-09-22, Code (PR #93 checklist) | r8 handover: `registerCandidate()` writes a DB row; `DeviceMetadata.imported_from_platform` column; idempotent migration | `registerCandidate()` in-memory for passive discovery; `registerPersistentCandidate()` writes durable row for platform imports; provenance in `DeviceLifecycleRecord.extraAttributes.importedFrom/originalId/registeredAt`; no migration, second confirm returns 409 | `ratified deviation` | Amend D10 to the stored provenance value — **closed 2026-09-23, see C-93-1 note on ITER row 6**: the literal value is `SmartThingsImportProvider.platform()` → `"smartthings"`, `RingImportProvider.platform()` → `"ring"`, verified directly from source. (This follow-up previously said "once posted (W-10)" — that W-10 id has since been claimed by an unrelated backlog item, Warm/Cold retention; removed here to avoid the collision, not because the amendment itself changed) |
| DL-2026-09-23-02 | 2026-09-22, Code (guest-access check) | D12: `/view/{token}/**` served by a separate read-only controller | Client-side page calls the shared `/api/*` routes with `?t={token}`; GET-only rule plus scope-to-path map are the only barrier | `ratified deviation` — W-2 passed, all 49 guest-token tests green ([PR #100](https://github.com/ImpressiveLLC/FaceoftheCabin/pull/100), merged) | D12 amended in `decisions/ontology-decisions.md` to describe the built shape |
| DL-2026-09-23-03 | 2026-09-23, Cowork | claude.ai project doc `ontology/cabin-ontology-decisions.md`: D14 = Energy, D15 = Reporting Topics | Living Ontology and this repo: D14 = Kiosk Auth, D15 = Energy, D16 = Reporting Topics | `defect` (in the project copy) | Retire the project copy (DOC-5) |
| DL-2026-09-23-04 | 2026-09-23, Cowork | Code handover r8 architecture reminders cite "D16 Topic-picker" and "D15 area write path" | Consistent with the Living Ontology numbering, inconsistent with the project copy of the ontology | `ratified deviation` (handover is correct) | None; resolved by DOC-5 |
| DL-2026-09-23-05 | 2026-09-23, Code | [SO-2026-09-23-r1](signoffs/SO-2026-09-23-r1.md) §2 lists `camera-events-bulk-clip-download` as "no PR yet" and `frigate-front-door-motion-sensitivity` as "no PR seen" | Both existed before the sign-off was issued: [PR #96](https://github.com/ImpressiveLLC/FaceoftheCabin/pull/96) (bulk clip download, open, `MERGEABLE`) and [PR #97](https://github.com/ImpressiveLLC/FaceoftheCabin/pull/97) (Frigate tuning, open, `MERGEABLE`), both opened 2026-09-22/23 before this sign-off's own "evidence reviewed" cutoff | `defect` (in the sign-off's evidence row, not in the code) | The §2.3 and §2.5 rulings (APPROVED WITH CONDITIONS; GATED on W-5) apply to these existing PRs directly — no new PR is opened for either item. Sign-offs are append-only ([governance/README.md](README.md#ownership)), so this is logged here rather than editing SO-2026-09-23-r1.md in place. ITER-2026-09-23.md row 7 ("Bulk clip") corrected the same way in Code's iteration acknowledgement |
| DL-2026-09-23-06 | 2026-09-23, Code | backlog.md W-4: "Remove or neutralize the 'insurance adjuster/mediator' callout anywhere a token holder can see it" | `GuestDashboard` (`App.jsx` — the entire component rendered at `/view/{token}`, the only surface a token holder ever sees) contains no "adjuster," "mediator" or "insurance" text anywhere in its render: title, hint text, alert/device/telemetry rows are all data-driven, no static callout. The only "insurance adjuster" string in the codebase is in the *admin's own* Settings → Access Links description (`App.jsx` line ~2284, `"Read-only links for people without a Google account — an insurance adjuster, a contractor."`), never sent to or rendered for a guest | `open` | W-4 as scoped may already be satisfied — nothing to remove on the token-holder side. Separate, smaller question for Nate/Cowork: should the admin-facing description also be reworded, since UC-1's actual subject is the policy-servicing agent, not the adjuster? Not changed pending an answer — it's admin-only copy, not a token-holder-visible callout, so it's outside W-4's literal scope either way |
| DL-2026-09-23-07 | 2026-09-23, Cowork (caught before merge) | W-1's exit evidence should be a response log proving each scope, nothing more | Code's ITER-2026-09-23.md row 4 (commit `33be4cf`, pushed to `main`-tracked but not-yet-merged branch `cowork/governance-skeleton`) posted the full live Tier 1 guest token in plaintext, scoped to `dashboard`/`device_states`/`alerts_read`/`observations_read`, in this public repo | `defect` — a real credential belongs in neither a commit nor a PR, redacted or not, full stop; last-4/label was the right call for identifying *which* token, not for handling a secret | **Revoke the token immediately** (Settings → Access Links) — a published secret is burned regardless of any later redaction. Redacted to `…a79e` in `33be4cf`'s successor commit. Squash-merging #98 keeps the plaintext out of `main`'s history; the branch commit itself remains fetchable by SHA on GitHub until well after the branch is deleted (dangling-commit retention, possible forks/clones already holding it) — rotation, not history rewriting, is the actual control here. Revoked by Nate 2026-09-23; this pin is closed |
| DL-2026-09-23-08 | 2026-09-23, Code (W-2, [PR #100](https://github.com/ImpressiveLLC/FaceoftheCabin/pull/100)) | backlog.md W-2: "`?t=` redacted in access logs" | No request-logging filter exists anywhere in the backend (`grep`-confirmed, zero matches for `RequestLoggingFilter`/`access.log`/etc.). `cabin-ui/nginx.conf` has no explicit `access_log`/`log_format` directive, so nginx's stock default format — which includes the full request line, query string included — is what actually runs. Nothing logs the token app-side; nginx's own container log almost certainly does, unredacted, by default | `open` | Needs an infra decision, not a backend code change: add an explicit `log_format` to `nginx.conf` that masks the `t` query param (or route guest requests through a header instead of `?t=` — a larger change touching `GuestDashboard`'s every `fetch`), or accept container-log-only exposure (not public, but still a real credential in a log file) as out of scope for now. Directly relevant given DL-2026-09-23-07 above — same class of exposure, smaller blast radius (container logs, not a public repo) but not yet closed either way |
