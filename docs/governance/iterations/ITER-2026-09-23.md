# ITER-2026-09-23 — Execution readiness

| Field | Value |
|---|---|
| Status | **Draft — Code to confirm feasibility; Nate to approve** |
| Governing sign-off | [SO-2026-09-23-r1](../signoffs/SO-2026-09-23-r1.md) |
| Backlog | [backlog.md](../backlog.md) |
| Definition of Done | [definition-of-done.md](../definition-of-done.md) (proposed; §1–10 of `docs/DEFINITION_OF_DONE.md` apply until approved) |
| Iteration goal | Send the UC-1 guest link safely, clear the merge lane, and preserve claim footage |

## Entry and exit criteria per item

"Entry" is what must be true before starting. "Exit" is what must be posted for the item to count as done.

| Order | ID | Owner | Entry | Exit (evidence) | DoD level |
|---|---|---|---|---|---|
| 1 | DOC-1 | Code commits Cowork's files | None | PR open from `cowork/governance-skeleton`; CI green | Iteration |
| 2 | #95 | Code | None | Merged | Iteration |
| 3 | W-8 | Nate; Code sizes disk | None | Free space on `/storage` reported; retention decision recorded in backlog | — (operator decision) |
| 4 | W-1 | Code | None | **Done 2026-09-23.** Response log, logged-out browser, test token ending `…a79e` (revoked after this test — see [DL-2026-09-23-07](../discrepancy-log.md)) (`dashboard`+`device_states`+`alerts_read`+`observations_read`): `/api/dashboard/config` 200, `/api/devices` 200 (197 entries), `/api/alerts/active` 200 (`generatedAt`/`alerts`/`counts`, empty at capture time), `/api/events/reported-fields` 200. `.guest-view` rendered, no nav rail, `location.pathname` confirmed `/view/{token}`. Full evidence in Code's turn transcript, not a separate file | Iteration |
| 5 | W-4 | Code | None | PR showing the callout absent from every token-visible view | Iteration |
| 6 | #93 | Code | C-93-1 posted | **Closed 2026-09-23 — satisfied from source, not from a live import.** Read-only `SELECT` on the M920q, 2026-09-23, found 0 rows — no SmartThings/Ring import has run in production (Vaultwarden one-time setup still pending). Cowork ruled this doesn't block C-93-1: `SmartThingsImportProvider.platform()` → `"smartthings"`, `RingImportProvider.platform()` → `"ring"` are compile-time constants, verified directly from source, and `PlatformImportController.confirm()`'s `Map.of("importedFrom", platform)` stores that exact value — no live row needed to know what it will be. #93 merged. See [DL-2026-09-23-01](../discrepancy-log.md) | Iteration |
| 7 | Bulk clip | Code | C-BC-1, C-BC-2 applied | Applied to existing [PR #96](https://github.com/ImpressiveLLC/FaceoftheCabin/pull/96) with the governance record added to its body; merged | Iteration |
| 8 | W-2 | Code | W-1 done | **Done 2026-09-23, [PR #100](https://github.com/ImpressiveLLC/FaceoftheCabin/pull/100), 9 new tests, 49/49 green** (`GoogleAuthInterceptorTest`): all-scopes guest token denied 403 on helpdesk/KB-write/presence/camera/managed-users/platform-info/access-tokens, and on a write to an otherwise-in-scope route; 401 distinguished from 403 with a fresh test. **`?t=` redaction: not satisfied, flagged not silently dropped** — no request-logging filter exists anywhere in the backend to redact from, and `cabin-ui/nginx.conf` has no explicit `access_log`/`log_format` override, so nginx's stock default (which includes the full query string) is what actually runs today. Needs an infra decision, not a backend change — see PR #100's own "Not covered" section. Real finding along the way: `WebConfig.java` wrongly claimed a guest token could reach `/api/helpdesk` — corrected in the same PR as documentation drift, not logged as a discrepancy | Iteration |
| 9 | W-3 | Code | W-2 done | Tests for all three codes plus the 5xx regression test; screenshots of the three UI states | Iteration |
| 10 | UC-1 release | Nate sends link; Cowork drafts message (CW-5) | W-1 to W-4 done **+ W-2b done** (added 2026-09-23 — SO-2026-09-23-r1 §3's own gate list names only W-1–W-4; sign-offs are append-only, so this addition is recorded here rather than editing that file); Cowork issues a follow-on sign-off | Link sent; token id and expiry recorded | Product |
| 11 | W-5 | Code | Frigate branch PR open | 24 h baseline posted before merge; 24 h after posted after merge | Iteration |
| 12 | W-6 | Code | Bulk clip merged | Manifest and zip verified on a 3-file download | Iteration |

W-7, DOC-2, DOC-3, DOC-4 and DOC-5 are ready to start once their "Blocked by" entries in the backlog clear. They are not committed to this iteration.

## Cowork / Code split

Per the artifact protocol, now applied to git:

- Cowork writes `decisions/`, `use-cases.md`, `backlog.md` scores, `definition-of-done.md`, `signoffs/` and `iterations/` drafts. Code commits them when Cowork cannot push.
- Code writes code, tests, and implementation-status lines, and proposes ad-hoc items with a score.
- Neither edits the other's lines. Disagreement goes to the discrepancy log.

## Code acknowledgement

Code confirms or corrects each Entry and Size above in its first reply, before starting item 3.
