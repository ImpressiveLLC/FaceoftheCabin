---
# Ensures Docker, the repo clone, and a registered GitHub Actions
# self-hosted runner all exist on the target host. Re-running this is safe:
# apt/package tasks no-op if already satisfied, the git clone becomes a
# fast-forward pull if the repo already exists, and the runner registration
# is skipped if a runner service is already active.

- name: Install Docker + Compose plugin
  ansible.builtin.apt:
    name:
      - docker.io
      - docker-compose-plugin
      - git
    state: present
    update_cache: true

- name: Ensure docker service is enabled and running
  ansible.builtin.systemd:
    name: docker
    enabled: true
    state: started

- name: Add deploy user to docker group
  ansible.builtin.user:
    name: "{{ ansible_user }}"
    groups: docker
    append: true

- name: Clone or update the FaceoftheCabin repo
  ansible.builtin.git:
    repo: "{{ repo_url }}"
    dest: "{{ repo_path }}"
    version: main
    force: false # never discard local changes on the host silently

# /home/{{ ansible_user }}, not /opt — confirmed against the real host:
# /opt itself is root:root 755, so even creating a new directory under it
# needs root. Installing into the deploy user's home avoids needing
# privilege escalation for anything except the two systemd steps at the end.
- name: Check whether a GitHub Actions runner is already registered
  ansible.builtin.stat:
    path: "/home/{{ ansible_user }}/actions-runner/.runner"
  register: runner_config
  become: false

- name: Download GitHub Actions runner package
  when: not runner_config.stat.exists
  ansible.builtin.get_url:
    url: "https://github.com/actions/runner/releases/download/v{{ github_runner_version }}/actions-runner-linux-x64-{{ github_runner_version }}.tar.gz"
    dest: "/home/{{ ansible_user }}/actions-runner.tar.gz"
    mode: "0644"
  become: false

- name: Create runner install directory
  when: not runner_config.stat.exists
  ansible.builtin.file:
    path: "/home/{{ ansible_user }}/actions-runner"
    state: directory
  become: false

- name: Extract runner package
  when: not runner_config.stat.exists
  ansible.builtin.unarchive:
    src: "/home/{{ ansible_user }}/actions-runner.tar.gz"
    dest: "/home/{{ ansible_user }}/actions-runner"
    remote_src: true
  become: false

- name: Register the runner (requires github_runner_token, see site.yml header)
  when: not runner_config.stat.exists
  become: false
  ansible.builtin.command:
    cmd: >
      ./config.sh --unattended
      --url https://github.com/{{ github_repo }}
      --token {{ github_runner_token }}
      --name {{ inventory_hostname }}
      --labels {{ 'cabin-m920q' if 'cabin' in group_names else 'home-hub' }}
      --work _work
    chdir: "/home/{{ ansible_user }}/actions-runner"
  no_log: true # the token must never land in playbook output/logs

# The only two steps that genuinely need root: svc.sh writes a systemd unit
# under /etc/systemd/system/ and enables it. become defaults to true at the
# play level (see site.yml) so these run with sudo as normal — everything
# above explicitly opted out with become: false instead.
- name: Install the runner as a systemd service
  when: not runner_config.stat.exists
  ansible.builtin.command:
    cmd: ./svc.sh install
    chdir: "/home/{{ ansible_user }}/actions-runner"

- name: Start the runner service
  when: not runner_config.stat.exists
  ansible.builtin.command:
    cmd: ./svc.sh start
    chdir: "/home/{{ ansible_user }}/actions-runner"
  # svc.sh (shipped by the runner package) wraps the generated systemd unit
  # name for us — safer than guessing the unit name ourselves for `systemd:`.
